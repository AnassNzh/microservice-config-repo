spring:
  application:
    name: service1
  h2:
    console:
      enabled: true
      path: /console2
server:
  port: 9001
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
